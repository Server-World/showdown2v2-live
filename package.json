{
  "name": "showdown2v2-live",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "cf:dry-run": "wrangler deploy --dry-run",
    "cf:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.122.0"
  }
}
