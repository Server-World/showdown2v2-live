(()=>{const tabs=[...document.querySelectorAll('[data-demo-tab]')],views=[...document.querySelectorAll('[data-demo-view]')];if(!tabs.length)return;tabs.forEach(tab=>tab.addEventListener('click',()=>{const target=tab.dataset.demoTab;tabs.forEach(item=>{const active=item===tab;item.classList.toggle('active',active);item.setAttribute('aria-selected',String(active))});views.forEach(view=>{const active=view.dataset.demoView===target;view.classList.toggle('active',active);view.hidden=!active})}))})();
