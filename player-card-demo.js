(()=>{const tabs=[...document.querySelectorAll('[data-demo-tab]')],views=[...document.querySelectorAll('[data-demo-view]')];if(!tabs.length)return;const activate=tab=>{const target=tab.dataset.demoTab;tabs.forEach(item=>{const active=item===tab;item.classList.toggle('active',active);item.setAttribute('aria-selected',String(active));item.tabIndex=active?0:-1});views.forEach(view=>{const active=view.dataset.demoView===target;view.classList.toggle('active',active);view.hidden=!active})};tabs.forEach((tab,index)=>{tab.addEventListener('click',()=>activate(tab));tab.addEventListener('keydown',event=>{let next=index;if(event.key==='ArrowRight')next=(index+1)%tabs.length;else if(event.key==='ArrowLeft')next=(index-1+tabs.length)%tabs.length;else if(event.key==='Home')next=0;else if(event.key==='End')next=tabs.length-1;else return;event.preventDefault();tabs[next].focus();activate(tabs[next])})});})();
