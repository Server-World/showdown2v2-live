(()=>{const tabs=[...document.querySelectorAll('[data-card-tab]')].filter(tab=>!tab.disabled);const views=[...document.querySelectorAll('[data-card-view]')];if(!tabs.length||!views.length)return;const activate=tab=>{const key=tab.dataset.cardTab;tabs.forEach(item=>{const active=item===tab;item.classList.toggle('active',active);item.setAttribute('aria-selected',String(active));item.tabIndex=active?0:-1});views.forEach(view=>{const active=view.dataset.cardView===key;view.hidden=!active})};tabs.forEach((tab,index)=>{tab.setAttribute('role','tab');tab.addEventListener('click',()=>activate(tab));tab.addEventListener('keydown',event=>{if(!['ArrowRight','ArrowLeft','Home','End'].includes(event.key))return;event.preventDefault();let next=index;if(event.key==='ArrowRight')next=(index+1)%tabs.length;if(event.key==='ArrowLeft')next=(index-1+tabs.length)%tabs.length;if(event.key==='Home')next=0;if(event.key==='End')next=tabs.length-1;tabs[next].focus();activate(tabs[next])})});activate(tabs.find(tab=>tab.classList.contains('active'))||tabs[0])})();
